services:
  - type: web
    name: miniroom-bot
    runtime: node
    plan: free
    buildCommand: npm install && npm run deploy
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: CLIENT_ID
        sync: false
      - key: GUILD_ID
        sync: false
      - key: DATA_CHANNEL_ID
        sync: false
